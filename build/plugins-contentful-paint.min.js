(self["webpackChunkmaneko"] = self["webpackChunkmaneko"] || []).push([["plugins-contentful-paint"],{

/***/ "./assets/js/plugins/contentful-paint.js":
/*!***********************************************!*\
  !*** ./assets/js/plugins/contentful-paint.js ***!
  \***********************************************/
/***/ (() => {

(function contentfulPaint() {
  // ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  //  options
  var options = {
    time_to_second: 0,
    time_to_third: 1400,
    onload_time_estimate: 1000,
    animation_time: 3000,
    animation_function: 'ease'
  };

  // ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  // preparation

  // add transition styles
  var styles = "\n        .js-1cp, .js-2cp, .js-3cp { \n            transition: opacity ".concat(options.animation_time / 1000, "s ").concat(options.animation_function, "; \n        }\n    ");
  var styleSheet = document.createElement('style');
  styleSheet.innerText = styles;
  document.head.appendChild(styleSheet);

  // ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  // start unhiding elements

  document.addEventListener('DOMContentLoaded', function () {
    // locate hidden els
    var first_elements = document.querySelectorAll('.js-1cp');
    var second_elements = document.querySelectorAll('.js-2cp');
    var third_elements = document.querySelectorAll('.js-3cp');

    // on doc ready, unhide First Contentful Paint
    unhideElements(first_elements);

    // in case window load fails
    setTimeout(function () {
      unhideElements(second_elements);
    }, options.time_to_second + options.onload_time_estimate);
    setTimeout(function () {
      unhideElements(third_elements);
    }, options.time_to_third + options.onload_time_estimate);
  });

  // on window load, unhide Second Contentful Paint
  window.addEventListener('load', function (event) {
    // locate hidden els
    var second_elements = document.querySelectorAll('.js-2cp');
    var third_elements = document.querySelectorAll('.js-3cp');
    setTimeout(function () {
      unhideElements(second_elements);
    }, options.time_to_second);
    setTimeout(function () {
      unhideElements(third_elements);
    }, options.time_to_third);
  });

  // ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  // aux functions

  function unhideElements(list) {
    // console.log('unhideElements', list)
    Array.from(list).forEach(function (el) {
      el.style.opacity = 1;
      setTimeout(function () {
        el.classList.remove('js-1cp');
        el.classList.remove('js-2cp');
        el.classList.remove('js-3cp');
      }, options.animation_time);
    });
  }
})();

/***/ })

}]);
//# sourceMappingURL=plugins-contentful-paint.min.js.map